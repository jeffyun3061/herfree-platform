# Initial Let's Encrypt issuance only (HTTP-01 via webroot).
# Replaced by herfree-staging.conf after the certificate exists.
server {
    listen 80;
    server_name api-staging.herpfree.co.kr;
    server_tokens off;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 404;
    }
}
