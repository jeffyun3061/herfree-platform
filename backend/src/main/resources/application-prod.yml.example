# Production profile template — use environment variables on EC2/RDS.
# Do NOT commit application-prod.yml or real secrets to git.
# Set SPRING_DATASOURCE_* and JWT_SECRET via EC2 env, Secrets Manager, or CI secrets.
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

jwt:
  secret: ${JWT_SECRET}
  access-expiration-seconds: ${JWT_ACCESS_EXPIRATION:3600}

# 운영 환경에서는 API 문서를 외부에 노출하지 않는다.
# Swagger UI와 OpenAPI spec endpoint가 열려 있으면 공격자에게 API 구조를 그대로 노출한다.
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
